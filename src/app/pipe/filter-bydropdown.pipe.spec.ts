import { FilterBydropdownPipe } from './filter-bydropdown.pipe';

describe('FilterBydropdownPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterBydropdownPipe();
    expect(pipe).toBeTruthy();
  });
});
